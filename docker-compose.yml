services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:2026.4.4
    ports:
      - "8123:8123"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ha-config:/config
      - ./custom_components/tesmart_kvm:/config/custom_components/tesmart_kvm:ro
    restart: unless-stopped

volumes:
  ha-config:
